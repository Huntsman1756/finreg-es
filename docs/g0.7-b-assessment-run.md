# G0.7-B — Real Assessment Run

Estado: **ejecutado, 21/21 MATCH** (assessment y reason). Run:
`fixtures/g0.7/runs/g0.7-001.json`, `result_sha`
`5da2e828db1dfa18a4f5ffe20b731916e5cb55f0f646178c18de64e987a42d19`.

## Inputs congelados (verificados en el propio run)

```text
corpus_sha256              fixtures/g0.5/corpus/entities.json
claims_ledger_sha256       fixtures/g0.6/claim-provenance-g0.5-a-003.json
derivation_ruleset_sha256  fixtures/g0.7/derivation-rules.json
assessment_cases_sha256    fixtures/g0.7/assessment-cases.json
derived_assertions_sha256  fixtures/g0.7/derived-assertions-g0.5-a-003.json
code_sha                   sha256 agregado de finreg_es/*.py
code_commit                1785352 (freeze de G0.7-A)
```

`frozen_input_integrity` verifica en cada ejecución que las derivadas
fueron producidas exactamente sobre los ficheros vivos. El replay es
offline y byte-idéntico (test con red bloqueada).

## Resultado

```text
CONFIRMED_AUTHORISED       8
INDETERMINATE              8
NO_ENTITLEMENT_EVIDENCED   5
CONFIRMED_NOT_AUTHORISED   0
matches                   21/21   reason_match 21/21
```

Distribución por probes:

- **Autorización CASP doméstica** (G07-001/002/003/004/006/008):
  `CONFIRMED_AUTHORISED` vía ESMA art. 63. G05-012 (LEI
  `INVALID_LENGTH`) y G05-015 (`INVALID_CHECK_DIGITS`) confirman por la
  vía del corpus: los LEI inválidos no soportan joins pero tampoco
  destruyen la evidencia ya vinculada.
- **FPS inbound** (G07-013/014): `CONFIRMED_AUTHORISED` vía listado
  CNMV ESI-FPS (`NOTIFICATION` + `FREEDOM_TO_PROVIDE_SERVICES`).
- **Estado no interpretable** (G07-005/007/009/010): `INDETERMINATE /
  UNINTERPRETABLE_EVIDENCE` — presencia EBA PSD2 con `ENT_AUT` como
  array de fechas. Incluye PI/EMI reales: el sistema se abstiene donde
  la fuente no permite distinguir vigente de retirada.
- **Clasificación ≠ enumeración** (G07-015): BdE IFM → `INDETERMINATE`;
  la regla estatutaria de servicios de pago no dispara sobre base
  `UNKNOWN` (G07-016 → `NO_ENTITLEMENT_EVIDENCED`).
- **Gap de vocabulario** (G07-012): AISP → `NO_ENTITLEMENT_EVIDENCED` +
  finding `UNSUPPORTED_ENTITY_CLASS` en derivación.
- **Pasaporte de solo intención** (G07-019): `ac_serviceCode_cou` DE →
  `INDETERMINATE` — la evidencia existe pero no establece efecto.
- **Caducidad de evidencia** (G07-020): as_of 2026-12-01 supera la
  política de 30 días de ESMA → `INDETERMINATE / ALL_EVIDENCE_STALE`.
- **Identidad no encontrada** (G07-021): `INDETERMINATE /
  IDENTITY_NOT_FOUND` — el gate nunca produce negativo.

## Trazabilidad por caso

Cada resultado registra `query`, `identity_resolution`,
`matching_assertion_ids` (aserciones que cubren la unidad),
`used_assertion_ids` (las que sostienen el veredicto),
`derived_assertion_ids`, `source_claim_ids` (ids del ledger G0.6),
`assertion_evaluations` completas (efecto efectivo en as_of, evidencia
con `raw_snapshot_sha256`) y los `expected_*` del preregistro con
`match`/`reason_match`.

## Criterios de cierre medidos sobre el run

```text
0 unsupported legal inferences      ✓ (findings clasificados, sin asercion inventada)
0 NOT_FOUND -> negative             ✓ (G07-021 INDETERMINATE)
0 register presence -> AUTHORISATION without rule   ✓ (toda asercion usada tiene rule_id)
0 invalid identifier joins          ✓ (LEI invalidos fuera del indice)
0 silent conflict resolution        ✓ (diagnostics preservan lo inadmisible)
0 expired entitlement -> negative   ✓ (no existe evidencia de expiracion;
                                       la unica caducidad es de evidencia -> INDETERMINATE)

100% EntitlementAssertions trazables a SourceAssertions  ✓
100% campos derivados trazables a reglas versionadas     ✓
100% casos reproducibles offline                         ✓
```

## Qué NO significa 21/21

El match es contra expectativas **preregistradas por razonamiento**,
no contra verdad absoluta: varios resultados correctos son
`INDETERMINATE` o `NO_ENTITLEMENT_EVIDENCED` porque la evidencia
congelada no sostiene más. G0.7-C audita cualquier divergencia (0 en
este run) y mantiene la disciplina de no editar expectativas tras ver
resultados.
